/**
 * Realtime synchronization is disabled to prevent background auto-sync
 * from overriding local canvas changes and page setup settings.
 */
export const useRealtimeSync = () => {
  // Auto-sync disabled
};

